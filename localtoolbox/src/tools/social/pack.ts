// Social & Media pack — implementations shared with the SEO pack.
import type { ComponentType } from "react";
import { socialTools } from "../seo-social/pack";

export const tools: Record<string, ComponentType> = socialTools;
