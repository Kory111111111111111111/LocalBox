// SEO & Web pack — implementations shared with the social pack.
import type { ComponentType } from "react";
import { seoTools } from "../seo-social/pack";

export const tools: Record<string, ComponentType> = seoTools;
